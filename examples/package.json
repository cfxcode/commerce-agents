{
  "name": "acme-examples",
  "private": true,
  "workspaces": [
    "web-shared",
    "*/storefront-web",
    "*/merchant-web"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check:i18n": "node ../scripts/check_retail_i18n.cjs"
  }
}
